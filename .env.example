# =========================================================================
# MindCare Therapy - Environment Variables (ตัวอย่าง)
# =========================================================================
# คำแนะนำ:
# 1. คัดลอกไฟล์นี้และเปลี่ยนชื่อเป็น ".env"
# 2. ใส่ API Key ของคุณในไฟล์ .env (ไม่ใช่ไฟล์นี้)
# 3. ไฟล์ .env จะถูกซ่อนโดย .gitignore เพื่อความปลอดภัย

# Gemini API Key (รับได้จาก https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=AIzaSyCG8Fy2v3RDT_PH9YgL5KEG0RKBUeoGDdo

# Model Name (ค่าเริ่มต้น: gemini-1.5-flash)
MODEL_NAME=gemini-1.5-flash

# Port สำหรับ Server (ค่าเริ่มต้น: 3000)
PORT=3000
